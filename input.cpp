#include <bits/stdc++.h>
#include <fstream>
using namespace std;

string AB; //ファイル名をAかBかを指定
int filenum; //ファイル名の番号

void input(){
    cin >> "入力するファイル名について" << endl;
    cin >> "A or B ? :" << AB << endl;
    cin >> "番号は ? :" << filenum << endl;


}